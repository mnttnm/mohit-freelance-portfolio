<div data-variation-content="minimal">

  <!-- Shader background -->
  <iframe class="mn-shader" src="/shaders/gilt-thread-minimal.html" loading="lazy" aria-hidden="true"></iframe>

  <!-- Header — just name and one link -->
  <header class="mn-header">
    <span class="mn-name">Mohit Tater</span>
    <a href="https://cal.com/tatermohit/call-with-mohit-tater" class="mn-cta-link" target="_blank" rel="noopener">Book a call</a>
  </header>

  <!-- Hero — one sentence, that's it -->
  <section class="mn-hero">
    <h1 class="mn-h1">I design and build products.<br/>Full stack. Ten years.</h1>
  </section>

  <!-- What you get — three lines -->
  <section class="mn-offer">
    <div class="mn-offer-item">
      <span class="mn-label">01</span>
      <span class="mn-value">You describe what you need. I build it. Design, code, deploy. 2-4 weeks.</span>
    </div>
    <div class="mn-offer-item">
      <span class="mn-label">02</span>
      <span class="mn-value">Your product isn't working. I find out why and fix it.</span>
    </div>
    <div class="mn-offer-item">
      <span class="mn-label">03</span>
      <span class="mn-value">Your team does too much manually. I build AI agents and workflows that handle it.</span>
    </div>
  </section>

  <!-- Work — real projects, one line each -->
  <section class="mn-work">
    <h2 class="mn-section-mark">Recent work</h2>

    <div class="mn-project">
      <span class="mn-project-name">Greetwood Shopify Store</span>
      <span class="mn-project-desc">Production e-commerce store for a furniture brand, custom theme, built in one session</span>
      <span class="mn-project-tag">Client / Shopify</span>
    </div>

    <a href="https://github.com/mnttnm/claude-skills" target="_blank" rel="noopener" class="mn-project">
      <span class="mn-project-name">Claude Code Skills</span>
      <span class="mn-project-desc">Agent skills that transform UI/UX of applications through automated grading and iteration</span>
      <span class="mn-project-tag">AI / Open Source</span>
    </a>

    <div class="mn-project">
      <span class="mn-project-name">Video-to-Requirements Pipeline</span>
      <span class="mn-project-desc">Loom recordings in, structured product specs out. Cut requirements gathering by 80%</span>
      <span class="mn-project-tag">Automation / AI</span>
    </div>

    <a href="https://mohit.stream" target="_blank" rel="noopener" class="mn-project">
      <span class="mn-project-name">learning.log</span>
      <span class="mn-project-desc">Digital garden. What I'm building, discovering, and figuring out. Public work-in-progress.</span>
      <span class="mn-project-tag">Astro / Personal</span>
    </a>
  </section>

  <!-- Testimonial -->
  <section class="mn-proof">
    <h2 class="mn-section-mark">What clients say</h2>
    <blockquote class="mn-quote">
      "After analyzing our goals, he highlighted assumptions and gaps in our thinking — and then recommended paths forward. This really accelerated our work."
    </blockquote>
    <span class="mn-attribution">Dean Teffer, VP of AI at Arctic Wolf</span>
  </section>

  <!-- CTA + Stack together -->
  <section class="mn-final">
    <div class="mn-final-top">
      <p class="mn-final-text">Taking on 1-2 projects.</p>
      <div class="mn-final-actions">
        <a href="https://cal.com/tatermohit/call-with-mohit-tater" class="mn-book" target="_blank" rel="noopener">Book a call</a>
        <a href="mailto:mohittater.iiita@gmail.com" class="mn-email">mohittater.iiita@gmail.com</a>
      </div>
    </div>
    <div class="mn-final-bottom">
      <div class="mn-stack-line">
        <span class="mn-stack-label">Stack</span>
        <span class="mn-stack-list">React, Next.js, TypeScript, Tailwind, Supabase, Vercel, Figma, Claude, n8n</span>
      </div>
      <div class="mn-stack-line">
        <span class="mn-stack-label">Based in</span>
        <span class="mn-stack-list">Bengaluru, India</span>
      </div>
    </div>
  </section>

  <footer class="mn-footer">
    <div class="mn-links">
      <a href="https://twitter.com/tatermohit" target="_blank" rel="noopener">Twitter</a>
      <a href="https://linkedin.com/in/tatermohit" target="_blank" rel="noopener">LinkedIn</a>
      <a href="https://github.com/mnttnm" target="_blank" rel="noopener">GitHub</a>
      <a href="https://mohit.stream" target="_blank" rel="noopener">learning.log</a>
    </div>
  </footer>

</div>

<style>
  /* ===== Minimal Variation ===== */
  [data-variation-content="minimal"] {
    --mn-bg: #FAFAFA;
    --mn-text: #111111;
    --mn-muted: #666666;
    --mn-faint: #AAAAAA;
    --mn-border: #E0E0E0;
    --mn-accent: #D93025;
    --mn-sans: 'Instrument Sans', -apple-system, BlinkMacSystemFont, sans-serif;

    background: var(--mn-bg);
    color: var(--mn-text);
    font-family: var(--mn-sans);
    min-height: 100vh;
    position: relative;
  }

  .mn-shader {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    border: none;
    z-index: 0;
    pointer-events: none;
    opacity: 0.5;
  }

  /* Ensure all content sits above the shader */
  .mn-header { z-index: 100; }
  .mn-hero, .mn-offer, .mn-work, .mn-proof, .mn-final, .mn-footer {
    position: relative;
    z-index: 1;
  }

  /* Header */
  .mn-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 48px;
    background: rgba(250, 250, 250, 0.9);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--mn-border);
  }

  .mn-name {
    font-weight: 600;
    font-size: 0.9rem;
    letter-spacing: -0.01em;
  }

  .mn-cta-link {
    font-size: 0.82rem;
    font-weight: 500;
    color: var(--mn-text);
    text-decoration: none;
    padding: 6px 16px;
    border: 1px solid var(--mn-text);
    border-radius: 4px;
    transition: background 0.15s, color 0.15s;
  }

  .mn-cta-link:hover {
    background: var(--mn-text);
    color: var(--mn-bg);
  }

  /* Hero */
  .mn-hero {
    padding: 160px 48px 80px;
    max-width: 720px;
    margin: 0 auto;
  }

  .mn-h1 {
    font-family: var(--mn-sans);
    font-size: clamp(2rem, 4.5vw, 3.2rem);
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: -0.03em;
  }

  /* Offer */
  .mn-offer {
    padding: 0 48px 80px;
    max-width: 720px;
    margin: 0 auto;
  }

  .mn-offer-item {
    display: flex;
    gap: 20px;
    padding: 20px 0;
    border-bottom: 1px solid var(--mn-border);
    align-items: baseline;
  }

  .mn-offer-item:first-child {
    border-top: 1px solid var(--mn-border);
  }

  .mn-label {
    font-size: 0.72rem;
    font-weight: 600;
    color: var(--mn-faint);
    flex-shrink: 0;
    width: 24px;
  }

  .mn-value {
    font-size: 0.95rem;
    color: var(--mn-muted);
    line-height: 1.5;
  }

  /* Work */
  .mn-work {
    padding: 60px 48px 80px;
    max-width: 880px;
    margin: 0 auto;
  }

  .mn-section-mark {
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--mn-faint);
    margin-bottom: 32px;
  }

  .mn-project {
    display: grid;
    grid-template-columns: 200px 1fr auto;
    gap: 16px;
    padding: 18px 0;
    border-bottom: 1px solid var(--mn-border);
    text-decoration: none;
    color: inherit;
    align-items: baseline;
    transition: background 0.15s;
  }

  a.mn-project:hover {
    background: rgba(0,0,0,0.02);
    margin: 0 -12px;
    padding: 18px 12px;
    border-radius: 4px;
  }

  .mn-project:first-of-type {
    border-top: 1px solid var(--mn-border);
  }

  .mn-project-name {
    font-weight: 600;
    font-size: 0.92rem;
  }

  .mn-project-desc {
    font-size: 0.88rem;
    color: var(--mn-muted);
    line-height: 1.4;
  }

  .mn-project-tag {
    font-size: 0.72rem;
    color: var(--mn-faint);
    white-space: nowrap;
    text-align: right;
  }

  /* Proof */
  .mn-proof {
    padding: 48px 48px;
    max-width: 880px;
    margin: 0 auto;
    border-top: 1px solid var(--mn-border);
  }

  .mn-proof .mn-section-mark {
    margin-bottom: 24px;
  }

  .mn-quote {
    font-size: 1rem;
    font-style: italic;
    color: var(--mn-muted);
    line-height: 1.7;
    margin-bottom: 12px;
    padding-left: 20px;
    border-left: 2px solid var(--mn-accent);
    max-width: 580px;
  }

  .mn-attribution {
    font-size: 0.8rem;
    color: var(--mn-faint);
    padding-left: 20px;
  }

  /* Final CTA + Stack */
  .mn-final {
    padding: 48px 48px 32px;
    max-width: 880px;
    margin: 0 auto;
    border-top: 1px solid var(--mn-border);
  }

  .mn-final-top {
    margin-bottom: 48px;
  }

  .mn-final-text {
    font-size: 1.4rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: 20px;
  }

  .mn-final-actions {
    display: flex;
    gap: 24px;
    align-items: center;
  }

  .mn-book {
    font-size: 0.88rem;
    font-weight: 600;
    color: #fff;
    background: var(--mn-text);
    padding: 10px 24px;
    border-radius: 4px;
    text-decoration: none;
    transition: opacity 0.15s;
  }

  .mn-book:hover {
    opacity: 0.85;
  }

  .mn-email {
    font-size: 0.85rem;
    color: var(--mn-muted);
    text-decoration: none;
    border-bottom: 1px solid var(--mn-border);
    transition: color 0.15s;
  }

  .mn-email:hover {
    color: var(--mn-text);
  }

  .mn-final-bottom {
    border-top: 1px solid var(--mn-border);
    padding-top: 24px;
  }

  .mn-stack-line {
    display: flex;
    gap: 16px;
    padding: 8px 0;
    align-items: baseline;
  }

  .mn-stack-label {
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--mn-faint);
    width: 64px;
    flex-shrink: 0;
  }

  .mn-stack-list {
    font-size: 0.82rem;
    color: var(--mn-muted);
  }

  /* Footer */
  .mn-footer {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 32px 48px;
    max-width: 880px;
    margin: 0 auto;
    font-size: 0.75rem;
    color: var(--mn-faint);
  }

  .mn-links {
    display: flex;
    gap: 24px;
  }

  .mn-links a {
    color: var(--mn-faint);
    text-decoration: none;
    transition: color 0.15s;
  }

  .mn-links a:hover {
    color: var(--mn-text);
  }

  /* ===== Responsive ===== */
  @media (max-width: 768px) {
    .mn-header {
      padding: 16px 24px;
    }

    .mn-hero {
      padding: 130px 24px 60px;
    }

    .mn-offer,
    .mn-work,
    .mn-proof,
    .mn-final {
      padding-left: 24px;
      padding-right: 24px;
    }

    .mn-project {
      grid-template-columns: 1fr;
      gap: 4px;
    }

    .mn-project-tag {
      text-align: left;
    }

    .mn-footer {
      padding-left: 24px;
      padding-right: 24px;
      flex-direction: column;
      gap: 12px;
      align-items: flex-start;
    }

    .mn-final-actions {
      flex-direction: column;
      align-items: flex-start;
      gap: 16px;
    }
  }
</style>
